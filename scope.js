const bonus = 10;
function sum(first, second){
    let result = first + second + bonus;

    if (result > 9) {
        var mood = "happy"
        mood = "mess"
        mood = "sad"
        console.log(mood);
    }
    return result;
}
const output = sum(5, 5);
console.log(output);


// console.log(day);
var day = 'friday'
console.log(day);


// let = this value can be change; but we can't call it outside the function.
// const = one man word!
//  var = its more flexible.

