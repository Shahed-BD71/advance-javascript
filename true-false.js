const age = 1;
if (age > 1) {
    console.log('its true')
}
else{
    console.log('its false')
}

// note: if the given number was not 0/we didnt set age value in if condition, it always tell us "its true".
// if we set 0 as our given number it shows "its false"